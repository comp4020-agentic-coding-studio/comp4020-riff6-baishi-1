// The site's own version, shown in the footer so a share-back audience can
// tell which build they are looking at. Bump it with every visible change.
export const siteVersion = "0.1.1";

export const versionLine = `Site version ${siteVersion}`;
